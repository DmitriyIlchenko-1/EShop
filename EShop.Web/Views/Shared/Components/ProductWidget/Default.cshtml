@model EShop.Web.Models.Catalog.ProductWidgetComponentModel

<h2>@Model.WidgetName</h2>

<div class="row">
    @foreach (var product in Model.Products)
    {
        <div class="col-xs-6 col-md-3">
            @await Html.PartialAsync("_ProductThumbnail", model: product)
        </div>
    }
</div>